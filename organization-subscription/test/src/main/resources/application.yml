app-config:
  authorization-server:
    type: keycloak
    base-url: ${KEYCLOAK_BASE_URL}
    client-id: ${KEYCLOAK_CLIENT_ID}
    client-secret: ${KEYCLOAK_CLIENT_SECRET}
  url:
    - organization-subscription: ${ORGANIZATION_SUBSCRIPTION_URL}
    - optimization: ${OPTIMIZATION_URL}
